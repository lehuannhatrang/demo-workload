# Remediation proposal

- Issue key: `7b972cb972`
- Risk: **medium**
- Source: `argocd`
- Target: `apps/payments-api` in `lehuannhatrang/demo-workload`

## Summary

Proposed remediation for HighPodMemory

## Root cause hypothesis

The single most likely root cause is:  

**The pod `payments-api-b5dcc897-cqwj5` ran a memory-intensive `stress` workload (`--vm-bytes 50M`) that caused its memory working set to exceed 75% of its 64MiB limit, triggering the `HighPodMemory` alert and subsequent kubelet-initiated termination due to memory pressure.**

This is supported by:
- The pod was actively running the `polinux/stress:latest` image with arguments designed to allocate and hold 50MB of memory.
- The configured memory limit (64Mi ≈ 67MB) left minimal headroom for system overhead, allowing the working set to briefly exceed 75% of the limit despite the nominal 50MB usage.
- Events confirm the pod was killed (`Reason: Killing`) at the time the alert fired, consistent with kubelet enforcement of memory limits.
- The deployment has since recreated the pod, indicating the issue was transient and tied to the specific replica’s resource exhaustion.

## Validation

all checks passed

- PASS `hypothesis_present` — RCA produced a hypothesis
- PASS `context_present` — context available
